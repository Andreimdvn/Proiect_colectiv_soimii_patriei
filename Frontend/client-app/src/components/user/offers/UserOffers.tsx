import * as React from "react";
import { inject, observer } from "mobx-react";
import { ViewStore } from "src/store/view-store";

@observer
export class UserOffers extends React.Component {
  render() {
    return <div>UserOffers</div>;
  }
}
